
import { DataItemType } from '../[input]Item_Schema/SchemaInputItemTypes';
// Shared base state for all slices

// Data slice types
export default interface ModelDataTypes  {
  YieldItems: DataItemType[];
} 
